version: '3'

services: 

  mysqldb:
    image: mysql:5.6
    container_name: mysqldb
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: tpachato
      MYSQL_ROOT: root
      MYSQL_PASSWORD: root
      MYSQL_HOST: mysqldb
    ports:
      - 3307:3306    
    volumes:
      - /home/mysql/data:/var/lib/mysql --user 1000

  app:
    image: "saafghassen/facture:latest"
    container_name: spring_app
    ports:
      - "8089:8089"
    environment:
      SPRING_DATASOURCE_URL: jdbc:mysql://mysqldb:3306/tpachato?createDatabaseIfNotExist=true&useUnicode=true&useJDBCCompliantTimezoneShift=true&useLegacyDatetimeCode=false&serverTimezone=UTC
      SPRING_DATASOURCE_USERNAME: root
      SPRING_DATASOURCE_PASSWORD: root
      MYSQL_USER: root
      MYSQL_ROOT_PASSWORD: root
      MYSQL_PASSWORD: root
      MYSQL_DATABASE: tpachato
    restart: on-failure
    links:
      - mysqldb
  frontend_app:
    image: "saafghassen/facturefront:latest"
    container_name: angular_app
    ports:
      - "4200:80"
    environment:
      BACKEND_URL: 192.168.1.150:8089
    restart: on-failure

