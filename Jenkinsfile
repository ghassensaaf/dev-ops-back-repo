pipeline {
  agent any

  stages {
    
    }
    stage('MVN Package') {
      steps {
        sh 'mvn --version';
        sh 'java -version';
        sh 'mvn package -e';
      }
    }
  }
}
